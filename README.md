# E-W Hotel collection website

## Code Format

Open terminal and run the following command:

```bash
npx prettier --write . '!**/*.{js,css}'
```

You may encounter some issues when formatting codes, fix them.
